import nltk
from nltk.corpus import brown

# Make sure NLTK knows where the data is
nltk.data.path.append("C:/nltk_data")

# Create Conditional Frequency Distribution
cfd = nltk.ConditionalFreqDist(
    (genre, word)
    for genre in brown.categories()
    for word in brown.words(categories=genre)
)

# Print genres
print(cfd.conditions())

# Tabulate frequencies
cfd.tabulate(
    conditions=['government', 'humor', 'reviews'],
    samples=['leadership', 'worship', 'hardship']
)

# Plot frequencies
cfd.plot(
    conditions=['government', 'humor', 'reviews'],
    samples=['leadership', 'worship', 'hardship']
)

# Cumulative table
cfd.tabulate(
    conditions=['government', 'humor', 'reviews'],
    samples=['leadership', 'worship', 'hardship'],
    cumulative=True
)

# News genre frequency distribution
news_fd = cfd['news']

print(news_fd.most_common(3))
print(news_fd['the'])
